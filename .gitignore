# Dependencies — reinstalled automatically
node_modules/

# Built frontend — rebuilt on deploy
public/

# SQLite database — never commit your data
server/db/

# Python bytecode
__pycache__/
*.pyc

# Environment files — may contain secrets
.env
.env.local

# OS files
.DS_Store
Thumbs.db

# Vite cache
client/.vite/
client/src/App(b).jsx
server/database(a).js
server/server(a).js
client/src/components/project/UValueLibrary(b).jsx
server/server(b).js
server/database(b).js
client/src/App(c).jsx
